/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    ...nextConfig,
    images: {
        domains: ['lh3.googleusercontent.com', 'firebasestorage.googleapis.com'],

    }
}
